name: PHP Composer

on:
  push:
    branches: [ "*" ]
  pull_request:
    branches: [ "*" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.4'
        extensions: gd, intl, iconv, sqlite3, pdo_sqlite, zip
        coverage: none
    - name: Install dependencies
      run: composer install --prefer-dist --no-progress --ignore-platform-reqs
    - name: Laravel Pint
      run: php vendor/bin/pint --test
    # Baselined, so this enforces no *new* findings. OOMs at the default 128M.
    - name: PHPStan
      run: php vendor/bin/phpstan analyse --memory-limit=1G

  security-audit:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.4'
        extensions: gd, intl, iconv, sqlite3, pdo_sqlite, zip
        coverage: none
    # Version comes from the packageManager field; passing it here too is a hard error.
    - name: Setup pnpm
      uses: pnpm/action-setup@v4
    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version-file: .nvmrc
        cache: pnpm
    - name: Install dependencies
      run: composer install --prefer-dist --no-progress --ignore-platform-reqs
    # Fails the build on any known advisory, so vulnerable deps surface here
    # rather than in a Dependabot backlog.
    - name: Composer audit
      run: composer audit
    - name: pnpm audit
      run: pnpm install --frozen-lockfile && pnpm audit
